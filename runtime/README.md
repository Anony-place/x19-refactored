# Runtime layer

The runtime layer is deliberately separate from the operator UI. It provides
skills, recall, delegation and cron primitives; `run.py` exposes them without
forcing the full-screen workspace.
