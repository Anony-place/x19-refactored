# Runtime fixes

- `target <host>` in the interactive terminal routes to `X19.autonomous_loop`.
- `x19_os_detect` provides multi-signal OS-family fingerprinting.
- `x19 os <target>` runs the fingerprint directly.
- The cognitive contract explicitly directs the model to use X19's native tools.
