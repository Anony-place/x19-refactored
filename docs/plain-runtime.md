# Plain runtime

`run.py` is the entry point. The default surface is plain terminal output.
Provider management is available through `setup` and `provider` commands.
Security assessments continue through `run`; the existing `dash` interface is explicit.
