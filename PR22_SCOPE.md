# PR scope

This change set intentionally does not replace X19's security engines.

It fixes the operator/runtime layer first:

- plain terminal default
- deterministic provider setup and verification
- live model discovery
- cognitive decision contract

The existing dashboard remains explicit and optional.
