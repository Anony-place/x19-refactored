# Runtime follow-up

The follow-up branch fixes the remaining operator-path issue: a plain `target <host>` line in the interactive console now invokes X19's assessment path instead of generic model chat.

It also adds evidence-driven OS fingerprinting with optional Nmap support and a direct `x19 os <target>` command.
