#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.x19
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: Anony-place/x19-refactored:latest
    container_name: x19
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.x19:/opt/data
    environment:
      - X19_UID=10000
      - X19_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: Anony-place/x19-refactored:latest
    container_name: x19-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.x19:/opt/data
    environment:
      - X19_UID=10000
      - X19_GID=10000
      - X19_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
