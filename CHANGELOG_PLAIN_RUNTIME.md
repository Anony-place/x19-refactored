# Plain runtime change set

- default `run.py` is plain terminal status/control
- provider setup discovers live models where the provider exposes them
- provider activation requires a real verification request
- custom OpenAI-compatible endpoints can be verified without a fake cloud key
- cognitive decision contract is installed before the agent imports its prompt helper
- explicit legacy `dash` remains opt-in
